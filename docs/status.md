# 当前开发状态

更新时间：2026-09-10

## 已完成

- 本地前后端工程和 SQLite 数据库。
- 视频、UP 主解析与真实接口采集。
- 视频和 UP 主快照存储。
- 后台自动采集与页面自动刷新。
- 趋势图增量/总量模式。
- 纵坐标格式化和大数可读显示。
- 监控对象停止确认。
- UID 长数字兼容。
- UP 主接口从 `space/acc/info` 增加 `web-interface/card` 兼容路径。
- VS Code 启动配置。
- 搜索预览确认、仅查看一次和后台监控选择。
- 当前监控与历史记录分区、历史对象恢复监控。
- 立即采集、采集间隔设置、采集状态字段。
- 采集失败重试和退避。
- 专业化浅色工作台 UI 与前端组件拆分。
- 趋势 24 小时/7 天/30 天范围切换。
- 趋势单指标、增量/总量和单点状态展示。
- 页面内搜索、采集和趋势错误提示。
- 首屏高度、趋势横坐标、B 站图片代理和按钮样式统一修复。

## 已知待完善

- 需要补充更多真实接口场景的集成测试。
- 采集数据源仍可能受到哔哩哔哩接口限流影响。
- 前端交互仍需在本地浏览器完成一次完整人工验收。

## 当前验证

- Python 代码编译通过。
- FastAPI 健康接口通过。
- SQLite 自动创建通过。
- 前端生产构建通过。
- 已验证真实视频和真实 UP 主 UID 解析。
- 基础输入解析自动化测试已加入 `backend/tests/test_core.py`。
- 趋势默认查询最近 7 天，历史快照窗口可查询至 30 天。
- 前端生产构建通过：`pnpm run build`。
- 后端测试通过：`python -m unittest discover -s tests -v`（4 项）。
- 顶部品牌标识已替换为本地 Bilibili 风格 SVG 图标。
- 详情区已移除与左侧监控列表重复的对象缩略图、类型和标识信息。
- 指标卡已补充增长值时间范围说明，趋势图日期标签已去重。
- 指标卡已改为“当前值 + 涨跌方向 + 变化量 + 统计周期”布局。
- 搜索框已明确支持 BV、AV、视频链接、UID 和空间链接，不暗示支持昵称或关键词搜索。
- 搜索框已直接显示 `支持 BV / AV / UID`，并在悬停提示中列出完整输入格式。
- 搜索框文案已调整为“输入 BV / AV / UID 或粘贴链接”，适配窄屏显示。
- 搜索框已移除重复分类提示，保留单一完整输入引导。
- 搜索框主提示已缩短为 `BV / AV / UID 或链接`，完整格式通过悬停提示查看。
- UP 主当前指标增加“上次投稿”时间，历史数据库会自动补充字段。
- 上次投稿接口已切换到 WBI 投稿接口；未配置 Cookie 时 B 站可能返回未登录，页面保留暂无记录提示。
